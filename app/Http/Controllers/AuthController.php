<?php

// AuthController removed — authentication is being moved to a React frontend (JSX/CSS).
// File intentionally left minimal to avoid interfering with your React setup.

namespace App\Http\Controllers;

class AuthController extends Controller
{
    // Intentionally empty placeholder
}
